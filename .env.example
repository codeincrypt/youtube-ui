# YouTube Data API v3 key (https://console.cloud.google.com/apis/credentials)
# Without it the app renders the bundled demo feed instead.
YOUTUBE_API_KEY=
# Optional: region for the "most popular" chart
YOUTUBE_REGION=US
